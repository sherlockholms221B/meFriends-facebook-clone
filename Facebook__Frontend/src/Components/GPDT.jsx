import React from 'react'
import DynamicMenu from './DynamicMenu/DynamicMenu'

const GPDT = () => {
  return (
    // Create Group,Shortcut and friends details box
    <div className='text-red-500 p-10'>GPDT</div>
  )
}

export default DynamicMenu(GPDT)
